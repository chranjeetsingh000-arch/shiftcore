import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import PageTransition from "@/components/ui/PageTransition";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Travel OS — Never Overpay For Travel Again",
  description:
    "The world's most trusted travel intelligence platform. Verified deals, hidden fee detection, AI-powered savings, and community knowledge — all in one place.",
  keywords:
    "cheap flights, travel deals, hotel deals, travel hacks, cashback travel, flight deals UK",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-white text-gray-900">
        <PageTransition>{children}</PageTransition>
      </body>
    </html>
  );
}
